<div class="blooms">
  <div class="bloom bloom-top"></div>
</div>

<style>
  .blooms {
    position: absolute;
    inset: 0;
    pointer-events: none;
    z-index: 0;
  }
  .bloom {
    position: absolute;
  }
  /* Thin red glow spanning the full width across the very top */
  .bloom-top {
    top: 0;
    left: 0;
    width: 100%;
    height: 520px;
    background: linear-gradient(
      to bottom,
      #390000 0%,
      rgba(57, 0, 0, 0.55) 30%,
      rgba(57, 0, 0, 0.22) 58%,
      rgba(57, 0, 0, 0.06) 82%,
      transparent 100%
    );
    filter: blur(54px);
  }
</style>
