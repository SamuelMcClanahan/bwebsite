<div class="blooms">
  <div class="bloom bloom-top"></div>
</div>

<style>
  .blooms {
    position: absolute;
    inset: 0;
    pointer-events: none;
    z-index: 0;
  }
  .bloom {
    position: absolute;
  }
  /* Darkest in the top-left corner, fading across the entire top edge */
  .bloom-top {
    top: 0;
    left: 0;
    width: 100%;
    height: 60%;
    background: radial-gradient(
      125% 90% at 0% 0%,
      rgba(157, 58, 53, 0.34) 0%,
      rgba(157, 58, 53, 0.12) 38%,
      transparent 72%
    );
    filter: blur(50px);
  }
</style>
